{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "ConnectionStrings": {
    "DefaultConnection": "Data source=datingapp.db"
  }
}//we have created below connection string set to DefaultConnection AS file name for local stored database by sqllite named datingapp.db
  // this datingapp.db file will be autocreated once we run proj.
//This setting will be running when we are in development mode and hosting environment : Development
  // but when we are in production it will be running appsetting.json
